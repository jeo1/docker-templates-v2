name: Merge Action

on:
  pull_request:
    types:
      - closed

jobs:
  merged-action:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged
        